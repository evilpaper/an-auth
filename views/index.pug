extends base

block vars
  - var title = 'Home';

block body
  section
    h1 Welcome
    h1 to An Auth.
    p.description An tiny application to learn everything
    p you need to know about authentication.
    p.tagline Please #[a(href="/login") login] or #[a(href="/register") create account] to continue.
